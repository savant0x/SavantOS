# SavantOS factory initramfs: virtio-only, compressed small for fast boot.
MODULES=(virtio_pci virtio_mmio virtio_blk virtio_gpu virtio_input virtio_console virtio_rng virtio_net)
BINARIES=()
FILES=()
HOOKS=(base udev modconf kms keyboard keymap block filesystems fsck)
COMPRESSION="zstd"
